{
  "name": "savant-gateway",
  "version": "2.0.0",
  "description": "Host-native AI provider gateway for the Savant ecosystem",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
